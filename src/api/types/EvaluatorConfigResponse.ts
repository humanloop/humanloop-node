/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Humanloop from "../index";

export interface EvaluatorConfigResponse {
    /** String ID of config. Starts with `config_`. */
    id: string;
    /** Other parameters that define the config. */
    other?: Record<string, unknown>;
    type: "evaluator";
    /** The user who created the config. */
    createdBy?: Humanloop.BaseModelsUserResponse;
    /** Whether the config is committed or not. */
    status: string;
    /** Name of config. */
    name: string;
    /** Description of config. */
    description?: string;
    /** Type of evaluator. */
    evaluatorType: string;
    /** The model config defining the LLM evaluator. */
    modelConfig?: Humanloop.ModelConfigResponse;
    /** The code for the evaluator. This code will be executed in a sandboxed environment. */
    code?: string;
    /** Whether this evaluator is target-free or target-required. */
    argumentsType?: Humanloop.EvaluatorArgumentsType;
    /** The type of the return value of the evaluator. */
    returnType?: Humanloop.EvaluatorReturnTypeEnum;
}
